<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
</head>
<body>
    <h1><%=title%></h1>
    <p><a href="/">All Books</a></p>
    <p><a href="/genres">Browse by Genre</a></p>
    <p><a href="/addBook">Add a new book</a></p>
    <% if (books.length>0) {%>
    <ul>
      <% books.forEach( book => {%>
        <li>
        <h2><%=book.title%></h2>
        <p>Author : <%=book.author%></p>
        <p>Genre : <%=book.genre%></p>
        <p>Description : <%=book.description%></p>
        <form action="/delete/<%=book.id%>" method="POST">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this book?')">delete book</button></form>
        <a href="/update/<%= book.id %>">
        <button type="button">update book</button>
         </a>        
        </li>
        <%})%>
    </ul>
   <%}%> 
</body>
</html>